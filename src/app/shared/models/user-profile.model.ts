// See list in src/app-config.json of where to update when adding/removing urls
export interface UserProfile {
  email?: string;
  email_verified?: boolean;
  family_name?: string;
  given_name?: string;
}
